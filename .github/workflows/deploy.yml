name: Deploy to itch.io
on: push

jobs:
  itchio-upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Emscripten
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: 5.0.7

      - name: Build WASM
        run: |
          mkdir -p web
          emcc $(ls *.c | tr '\n' ' ') -o web/index.html --shell-file shell.html --use-port=sdl3 -s WASM=1
          zip -j game.zip web/index.html web/index.wasm web/index.js

      - name: Setup Butler
        uses: remarkablegames/setup-butler@v2

      - name: Push to itch.io
        run: butler push game.zip tulioemmanuel/4-jetpack-joyride:html5
        env:
          BUTLER_API_KEY: ${{ secrets.ITCH_IO_API_KEY }}
